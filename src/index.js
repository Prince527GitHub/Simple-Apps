const{app:app,BrowserWindow:BrowserWindow,ipcMain:ipcMain,dialog:dialog}=require("electron"),path=require("path");require("electron-squirrel-startup")&&app.quit();const createWindow=()=>{const mainWindow=new BrowserWindow({width:800,height:600,resizable:!1,autoHideMenuBar:!0,fullscreenable:!1,webPreferences:{nodeIntegration:!0,contextIsolation:!1}});mainWindow.loadFile(path.join(__dirname,"index.html"))};app.on("ready",createWindow),app.on("window-all-closed",()=>{"darwin"!==process.platform&&app.quit()}),app.on("activate",()=>{0===BrowserWindow.getAllWindows().length&&createWindow()}),ipcMain.handle("open-folder-dialog",async()=>{const files=await dialog.showOpenDialog({properties:["openDirectory"]});return files.canceled?null:files.filePaths});